<!-- Write and run tests for the current changes. -->
Review the current changes with git_diff. Identify which functions/modules were modified. Write appropriate unit tests for the changed code. Run the test suite using bash. Report pass/fail status and any failures to fix.
