<!-- Diagnose and fix the last error or failing test. -->
Run the last failing command or test suite using bash to reproduce the error. Read the relevant source files. Identify the root cause. Implement a fix. Verify the fix by running the tests again.
